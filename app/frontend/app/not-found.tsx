import { CenterMessage } from "@/components/ui";

// An unknown address inside the Teams tab: one sentence and the way back,
// not the framework's bare 404.
export default function NotFound() {
  return <CenterMessage>No page at this address.</CenterMessage>;
}
